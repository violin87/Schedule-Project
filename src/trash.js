  
      // const updateSchedule = () => {
  //   if (startSeminar || endSeminar || students.length != 0) return;
    
    
    // const breakStartDecimal = breakStart ? convertToDecimal(breakStart) : null;
    // const breakEndDecimal = breakEnd ? convertToDecimal(breakEnd) : null;
  
    // const totalSeminarMinutes =
    //   (endSeminarDecimal - startSeminarDecimal) * 60 -
    //   (breakEndDecimal && breakStartDecimal ? (breakEndDecimal - breakStartDecimal) * 60 : 0);



// Skip break time if needed
      // if (breakStartDecimal && breakEndDecimal && currentTime > breakStartDecimal && currentTime < breakEndDecimal) {
      //   currentTime = breakEndDecimal;
      // }



//   const convertToDecimal = (time) => {
//     const [hour, minute] = time
//       .split(':')
//       .map((val) => parseInt(val.replace(/\D/g, ''), 10));
//     const isPM = time.toLowerCase().includes('pm');
//     return (isPM && hour < 12 ? hour + 12 : hour) + minute / 60;
//   };
